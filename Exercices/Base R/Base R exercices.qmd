---
title: "Base R exercices"
author: "Votre nom"
format: html
editor: visual
execute: 
  echo: true
---

# Série 1

**(1) Création de variables :** Créez deux variables `x` et `y` et attribuez-leur les valeurs 5 et 3 respectivement. Ensuite, calculez la somme, la différence, le produit et le quotient (division) de ces deux variables.

```{r}

```

**(2) Calculs avec des vecteurs :** Créez un vecteur `numbers` contenant les nombres de 1 à 10. Ensuite, calculez la somme de tous les nombres pairs dans ce vecteur.

```{r}

```

**(3) Chaînes de caractères :** Créez une variable `nom` contenant votre prénom et une variable `nom_de_famille` contenant votre nom de famille. Concaténez ces deux variables pour former votre nom complet.

```{r}

```

**(4) Fonctions mathématiques :** Utilisez la fonction `sqrt()` pour calculer la racine carrée d'un nombre de votre choix. Ensuite, utilisez la fonction `round()` pour arrondir le résultat à deux décimales.

```{r}

```

**(5) Conversion de types :** Créez une variable `age` contenant votre âge en tant que nombre entier. Ensuite, convertissez cette variable en caractère (chaîne de caractères) et ajoutez "ans" à la fin pour obtenir une phrase complète.

```{r}

```

# Série 2

**Créer une liste :** Créez une liste appelée `ma_liste` qui contient les éléments suivants :

-   Un vecteur de nombres de 1 à 5.

-   Un vecteur de caractères avec les noms de jours de la semaine.

-   Une matrice 2x2 avec des nombres de votre choix.

-   Une liste imbriquée contenant trois vecteurs de nombres.

```{r}

```

**Accéder aux éléments d'une liste :** Accédez au troisième élément de la liste `ma_liste`, qui est une matrice. Ensuite, accédez au deuxième élément du deuxième vecteur de nombres dans la liste imbriquée.

```{r}

```

**Ajouter un nouvel élément :** Ajoutez un vecteur de nombres de 6 à 10 à la fin de `ma_liste`.

```{r}

```

**Supprimer un élément :** Supprimez le deuxième élément de la liste `ma_liste`, qui est un vecteur de caractères contenant les noms des jours de la semaine.

```{r}

```

**Modifier un élément :** Modifiez le premier élément du premier vecteur de nombres dans la liste imbriquée. Remplacez-le par un nouveau nombre de votre choix.

```{r}

```

# Série 3

**(1) Fonction de calcul :** Créez une fonction appelée `calcul_somme` qui prend deux arguments (`a` et `b`) et renvoie leur somme. Appelez cette fonction avec différentes paires de nombres.

```{r}

```

**(2) Fonction avec une condition :** Écrivez une fonction appelée `est_pair` qui prend un nombre comme argument et renvoie `TRUE` si le nombre est pair et `FALSE` sinon. Testez la fonction avec plusieurs nombres.

```{r}

```

**(3) Boucle for :** Utilisez une boucle `for` pour afficher les nombres de 1 à 10 sur la console.

```{r}

```

**(4) Fonction avec boucle :** Créez une fonction appelée `table_de_multiplication` qui prend un nombre entier `n` comme argument et affiche la table de multiplication de ce nombre jusqu'à 10.

```{r}

```

**(5) Fonction récursive :** Écrivez une fonction récursive appelée `factorielle` qui calcule la factorielle d'un nombre entier `n` (n!). La factorielle de n est le produit de tous les entiers de 1 à n. Testez la fonction avec différentes valeurs de `n`.

```{r}

```
